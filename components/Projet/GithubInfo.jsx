import Svg from 'utils/svg';
import styled from '@emotion/styled';

export default function GithubInfo() {
	const GithubInfoStyled = styled.div`
		margin-top: 60px;
	`;
	return <GithubInfoStyled>GithubInfo</GithubInfoStyled>;
}
