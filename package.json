{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir && php bin/console cache:pool:clear vite_cache_pool",
    "serve": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.34.1",
    "vite": "^2.3.5"
  }
}
